<p id="notice"><%= notice %></p>

<h1>Listing Users</h1>
<%= form_tag edit_multiple_users_path, method: :get do %>
<table>
  <thead>
    <tr>
      <th>Mail</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @user.each do |user| %>
      <tr>
        <td><%= check_box_tag "users_ids[]", user.id %></td>
        <td><%= user.id %></td>
        <td><%= user.email %></td>
        <td><%= link_to 'Show', user %></td>
        <td><%= link_to 'Edit', edit_user_path(user) %></td>
        <td><%= link_to 'Destroy', user, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<%= submit_tag "Edit check" %>
<% end %>
<br>






       <!-- <%= form_tag buscar_afip_users_path, method: :get, remote: true do %>
        <div class="input-group">
          <%= text_field_tag :search_afip,  params[:search_afip], placeholder: "Buscar turnos", class: "form-control"%>
            <span class="input-group-btn">
            <%= submit_tag "Buscar", class: 'btn btn-default'%>
            </span>
        </div>
        <% end %>

                  <div id="afp"><p>fdsa</p></div>-->



<%= link_to 'New User', new_user_path %>

